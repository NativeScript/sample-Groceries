<Page loaded="loaded" xmlns:lv="nativescript-pro-ui/listview">
    <ActionBar title="Groceries"></ActionBar>
    
<GridLayout rows="auto, *">

<GridLayout id="addBar" row="0" columns="*, auto" class="add-bar">
    <TextField id="groceryTextField" text="{{ grocery }}" hint="Enter a grocery item" row="0" col="0"/>
    <Image src="res://add" tap="add" row="0" col="1"/>
</GridLayout>

    <lv:RadListView items="{{ groceryList }}" 
        row="1" 
        itemSwipeProgressStarted="onSwipeCellStarted($event)" 
        swipeActions="true"
        >
        <lv:RadListView.itemTemplate>
            <GridLayout class="grocery-list-item">
                <Label class="p-15" text="{{ name }}"/>
            </GridLayout>
        </lv:RadListView.itemTemplate>
    </lv:RadListView>

    <lv:RadListView.itemSwipeTemplate>
        <GridLayout columns="*,auto">
            <StackLayout id="delete-view" col="1" tap="delete($event)" class="delete-view">
                <Image src="res://delete" />
            </StackLayout>
        </GridLayout>
    </lv:RadListView.itemSwipeTemplate>

  <ActivityIndicator busy="{{ isLoading }}" row="1" horizontalAlignment="center" verticalAlignment="center" />

</GridLayout>

</Page>